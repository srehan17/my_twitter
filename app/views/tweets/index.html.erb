<% provide(:title, "Say Cheese!") %>
  <section class="section">
    <div class="container">
      <div class="columns">

        <div class="column is-3">
          <%= render 'leftbar' %>
        </div>

        <div class="column is-6">
          <%= render 'form' %>
          <hr/>
          <%= render 'feed' %>
        </div>

        <div class="column is-3">
          <%= render 'rightbar' %>
        </div>

      </div>
    </div>
  </section>